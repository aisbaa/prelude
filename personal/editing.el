;;; whitespace cleanup settings
(setq whitespace-style '(face
                         tabs
                         spaces
                         trailing
                         lines
                         space-before-tab
                         newline
                         empty
                         space-mark
                         tab-mark
                         newline-mark))
