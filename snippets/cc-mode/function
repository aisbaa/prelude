# name: neurotechnology function
# key: function
# --
/**
 * ${4:doc}
 *
 * $3
 */
${1:int}
${2:name}(${3:args})
{
	$1 retval = ${5:-1};

	$3

	$0

done:

	return retval;
}

(mapconcat '(lambda (elem) (print elem)) '"int argc, char **argv")